character_name = 'John'  # character_name is a variable and the value is assigned using assignment operator
character_age = 55
"""
For concatenation of int to string , we need to use str() and pass int variable
it will convert that to string and then concatenate , if we don't convert we will get runtime error as 
"TypeError: can only concatenate str (not "int") to str"
"""
print("There was a man named " + character_name + ",")
print("He was " + str(character_age) + " years old")
print("He likes the name " + character_name)
print("But he did not like being " + str(character_age))

# assigning new values to the existing variables
character_name = "Jim"  # string type - stores plain text
character_age = 36  # number - stores a whole number as well as decimal numbers

print("************************************************")
print("There was a man named " + character_name + ",")
print("He was " + str(character_age) + " years old")
print("He likes the name " + character_name)
print("But he did not like being " + str(character_age))
is_Male = True  # boolean - for true or false flag
"""
3 basic types of data
1. String
2. Number
3. Boolean
"""
