"""
There are many modules which comes with python installation.
Those are called built-in modules like e.g - datetime module

Apart from built in modules there are modules like third party packages which we need to install
Those are called 3rd party modules / external modules which is packaged inside a package

All the packages are stored in pypi repository
pypi - python package index

Module is a python file (name of the file)
package - is a collection of module
package always have files along with one constant file called _init_.py
That file is the which distinguishes a package form a directory
"""

"""
We install python packages using a package manager called pip
like npm - for node
for java - maven
If we install python 3 , automatically pip is installed
pip install django
pip install django
for mac - pip3 install django
"""
